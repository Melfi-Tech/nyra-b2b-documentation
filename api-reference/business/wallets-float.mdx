---
title: 'Get Business Float Wallets'
description: 'Retrieves all float wallets for the business associated with the authenticated API client'
openapi: 'GET /business/wallets/float'
---

## Overview

Retrieves all float wallets for the business associated with the authenticated API client.

## Authentication

- **Type**: API Client
- **Headers**:
  - `x-client-id`: The client ID (required, string)
  - `Authorization`: `Bearer <client_secret>` (required, string)

## Response

### Success Response (200 OK)

```json
{
  "success": true,
  "message": "Business float wallets fetched successfully",
  "data": [
    {
      "wallet_id": "string",
      "account_number": "string",
      "bank_name": "string",
      "owners_fullname": "string",
      "frozen": false,
      "business_id": "string",
      "isFloat": true,
      "created_at": "string",
      "updated_at": "string"
    }
  ]
}
```

## Error Handling

- **401 Unauthorized**: Invalid client ID or secret
- **403 Forbidden**: API client not authorized for the business
- **404 Not Found**: Business not found
