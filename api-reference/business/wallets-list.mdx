---
title: 'Get All Customer Sub-Wallets'
description: 'Retrieves all customer sub-wallets for the business associated with the authenticated API client'
openapi: 'GET /business/wallets/all'
---

## Overview

Retrieves all customer sub-wallets for the business associated with the authenticated API client. Excludes business float wallets.

## Authentication

- **Type**: API Client
- **Headers**:
  - `x-client-id`: The client ID (required, string)
  - `Authorization`: `Bearer <client_secret>` (required, string)

## Response

### Success Response (200 OK)

```json
{
  "success": true,
  "message": "Sub wallets fetched successfully",
  "data": [
    {
      "wallet_id": "string",
      "account_number": "string",
      "bank_name": "string",
      "owners_fullname": "string",
      "frozen": false,
      "business_id": "string",
      "isFloat": false,
      "created_at": "string",
      "updated_at": "string"
    }
  ]
}
```

## Error Handling

- **401 Unauthorized**: Invalid client ID or secret
- **403 Forbidden**: API client not authorized for the business
